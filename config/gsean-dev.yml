spring:
  application:
   name: myconfig-client
 
server:
   port: 8889
eureka:
 instance:
   hostname: localhost
 client:
   serviceUrl:
     defaultZone: http://${eureka.instance.hostname}:8061/eureka/


